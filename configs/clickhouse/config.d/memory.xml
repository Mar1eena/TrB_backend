<clickhouse>
    <!-- clickstack-all-in-one: ClickHouse + HyperDX + Mongo + OTEL.
         Не отдавать ~90% cgroup ClickHouse — иначе OOM у UI/коллектора. -->
    <max_server_memory_usage_to_ram_ratio>0.55</max_server_memory_usage_to_ram_ratio>
</clickhouse>
